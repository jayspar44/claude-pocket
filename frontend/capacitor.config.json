{
    "appId": "{{APP_ID_BASE}}.dev",
    "webDir": "dist",
    "appName": "{{PROJECT_TITLE}} (Dev)",
    "server": {
        "androidScheme": "https"
    },
    "plugins": {
        "Keyboard": {
            "resizeOnFullScreen": true,
            "style": "dark",
            "resize": "native"
        },
        "StatusBar": {
            "overlay": true
        },
        "SystemBars": {
            "insetsHandling": "disable"
        }
    }
}
